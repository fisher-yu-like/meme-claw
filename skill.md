### 一、整体架构概览

系统分为五个逻辑层：**用户交互层 → 主控调度层 → 专项能力层 → 工具与检索层 → 存储与定时层**。

```text
[ 用户交互 ]  (搜索/订阅/查看报告)
      ↓
[ 主控 Agent ] (意图识别、任务分发、上下文管理)
      ↓
[ 专项 Agent 集群 ]
├── 梗百科 Agent   (简介/来源/变体)
├── 视频猎人 Agent (多平台视频检索)
├── 文化人类学家 Agent (成因分析/学术报告)
└── 情报局 Agent   (定时爬取/热度监控)
      ↓
[ 工具与外部接口 ] (搜索引擎API、视频平台API、LLM、爬虫)
      ↓
[ 数据存储层 ] (梗知识库、用户订阅表、报告存档)
```

### 二、核心模块详细设计

#### 1. 主控 Agent（调度与上下文中枢）
- **职责**：接收用户查询，判断意图（是实时搜索还是订阅请求），将复杂任务拆解给子 Agent，并组装最终回复。
- **工作流示例**：
    1. 用户输入“告诉我什么是‘这很开门’”。
    2. 主控判定为 **搜索任务**，并行调用 `梗百科Agent` 和 `视频猎人Agent`。
    3. 获取基础信息后，询问用户“是否需要生成深度文化分析报告？”
    4. 若用户同意，唤醒 `文化人类学家Agent` 进行长文本生成。
    5. 最终推送一条整合卡片：**文字简介 + 来源出处 + 视频播放链接 + 报告PDF预览**。

#### 2. 专项 Agent 集群（能力中心）

| Agent 名称 | 核心能力 | 依赖工具/模型 | 输出产物 |
| :--- | :--- | :--- | :--- |
| **梗百科 Agent** | 检索梗的**定义、别名、起源贴吧/微博/抖音账号、关键时间节点** | 联网搜索 API、内部向量数据库 | 结构化简介卡片（Markdown） |
| **视频猎人 Agent** | 根据梗名寻找**最具代表性**的 B 站/抖音/YouTube 视频，提取 Embed 代码或直链 | 视频平台非官方搜索 API、Selenium 自动化 | 视频缩略图 + 标题 + 播放链接列表 |
| **文化人类学家 Agent** | **深度分析成因**：米姆传播学、社会情绪共鸣、模因变异机制；输出**学术报告** | **带长上下文能力的 LLM** (这里可以用户自己更换) + 专用 Prompt Chain | 学术报告（含摘要、文献综述式分析、传播模型预测） |
| **梗情报局 Agent** | 每日定时扫描热搜榜、NGA、贴吧、抖音热榜，发现**新型变异梗** | 爬虫脚本 + 热度阈值过滤器 | 新梗入库通知、推送简报 |

#### 3. 深入分析 & 学术报告生成机制（关键流程）
单纯的 Prompt 调用难以写出真正有洞见的“学术报告”，需要结合 **知识增强生成（RAG）**：

- **检索增强**：在生成报告前，Agent 先搜索与该梗相关的**社会事件背景**（例如：“这很开门”与鉴宝直播热的关系）以及**模因学理论论文摘要**。
- **分析框架 Prompt**：
    1. **模因溯源**：谁是第一个感染者（Originator）？在何种语境下诞生？
    2. **变异与选择**：梗图/文案发生了哪些二次创作？为何有些变体消亡，有些病毒式传播？
    3. **社会心理映射**：该梗反映了年轻群体的什么集体情绪（解构权威、自嘲、身份认同）？
    4. **生命周期预测**：处于爆发期、平台期还是衰退期？
- **输出格式**：自动生成带 **参考文献脚注** 和 **传播路径图谱** 的 Markdown 文档，并可导出为 PDF。

#### 4. 定时推送与存储方案

| 功能点 | 实现策略 |
| :--- | :--- |
| **定时任务** | 后台 Cron Job（如 APScheduler），每日凌晨 3 点唤醒 `梗情报局 Agent`。 |
| **新梗判定** | 比较“今日词频/播放量”与“昨日基数”，波动率 > 200% 且未存在于**内部梗库**中的词汇被标记为新梗。 |
| **用户订阅推送** | 维护 `User-Subscribe` 表。推送内容为简报：“昨夜新增潜在热词：XXX，当前热度指数：🔥🔥🔥”。支持邮件/Webhook。 |
| **数据存储** | **MongoDB**（适合存储梗的多变元数据结构）<br>字段设计：`meme_name`, `aliases`, `origin_url`, `videos: [{platform, url, embed}]`, `analysis_report_md`, `last_update_time`, `hot_index_history`。 |

### 三、用户交互流程示例（Mermaid 图逻辑描述）

1. **用户搜索** → 主控判断库内是否有缓存且未过期（< 7天）？
   - 是 → 直接返回缓存内容。
   - 否 → 触发 `梗百科` + `视频猎人` 并发执行 → 更新缓存。
2. **用户点击“生成学术报告”** → `文化人类学家` 启动长任务 → 主控轮询状态 → 完成后推送下载链接。
3. **用户点击“订阅每日新梗”** → 存储用户 ID 至推送队列。

### 四、关键技术选型建议（非代码）

- **Agent 编排框架**：**LangGraph** 或 **CrewAI**。
    - 理由：你的需求中有明显的**状态流转**（搜索 → 分析 → 推送）和**循环/等待**，LangGraph 的图结构比单纯的 ReAct 循环更稳定，且方便加入人工确认节点（生成报告前询问用户）。
- **视频搜索实现**：不建议依赖单一官方 API（大多不开放关键词检索视频详情），推荐使用 **Firecrawl** 或 **ScrapingBee** 抓取搜索引擎的 Video Tab 结果（如 Google 视频搜索 `site:bilibili.com 关键词`），或利用 B 站开放的搜索接口。
- **向量数据库**：用于存储已分析过的**梗的特征向量**，当新梗出现时，快速判断是否与旧梗是同一种模因的变体（如“V我50”与“肯德基疯狂星期四”的关系）。
- **LLM 选择**：建议使用支持长上下文的模型（如 GPT-4 Turbo 或者开源的 LLaMA 2 70B），以便在生成学术报告时能够处理大量背景信息和参考文献。